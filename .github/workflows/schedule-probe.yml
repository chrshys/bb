name: Schedule probe

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    name: Record scheduled trigger
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: date -u +%Y-%m-%dT%H:%M:%SZ
